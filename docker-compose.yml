version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      # Override DB path to use a named volume (persists across restarts locally)
      DB_PATH: /data/finance.db
    volumes:
      - db_data:/data

volumes:
  db_data:
